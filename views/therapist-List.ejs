<%- include('./includes/head.ejs') %>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/therapist-List.css">
</head>


<body>
    
  <%- include('./includes/navbar.ejs') %>

  <main style="margin-top: 100px;">
    <% if (usersAll.length > 0) { %>
        <div style="display: flex; justify-content: center; align-items: stretch; flex-wrap: wrap;">
        <% for (let singleUser of usersAll) { %>
            <% if (singleUser.role ==='therapist') { %>
            <div class="profile-card" style="margin-left: 30px; margin-right: 30px; margin-bottom: 60px;">
                <div class="img-area">
                    <div class="inner-area">
                        <img src="<%= singleUser.image %>" alt="" />
                    </div>
                </div>

                <div class="name"><%= singleUser.name %></div>
                <div class="about"><%= singleUser.name %></div>
                    <p style="margin: 15px;"></p>
                <div class="buttons text-center" style="display: inline-block;">

                    <form action="/therapists/<%=singleUser._id%>">
                        <button type="submit" style="margin-bottom: 10px;">View Profile</button>
                        <input type="hidden" name="therapistId" value="<%= singleUser.id %>">
                    </form>

                    <form action="/book" method="POST">
                        <button type="submit">Book</button>
                        <input type="hidden" name="userEmail" value="<%= singleUser.email %>">
                    </form>

                </div>
                </div>
                <% } %>
            <% } %>
            <% } %>
        </div>


      
    <!-- <main style="margin-top: 100px; ">
        <% if (usersAll.length > 0) { %>
            <div style="display: flex; justify-content: center; align-items: stretch; flex-wrap: wrap;">
                <% for (let singleUser of usersAll) { %>
                    <article class="card therapist-card" style="border-radius: 0px;">
                        <header class="card__header">
                            <h3 class="user__name"> <%= singleUser.name %> </h3>
                        </header>
                        <div class="card__image">
                            <img src="<%= singleUser.image %>" style="max-width: 100%; max-height: 100%;">
                        </div>
                        <div class="card__content">
                            <p class="user__email"><%= singleUser.email %></p>
                            <p class="user__role"> <%= singleUser.role %> </p>
                        </div>
                        <div class="text-center">
                            <div class="row text-center">
                                <div class="col-lg-6">
                                    <button class="btn btn-primary" style="width: 100%; height: 100%; border-radius: 0; ">Book Now</button>
                                </div>
                                <div class="col-lg-6">
                                    <button class="btn btn-secondary" style="width: 100%; height: 100%; border-radius:0;">View Profile</button>
                                </div>
                        </div>

                    </article>
                    <% } %>
            </div>


            <section class="pagination">
                <% if (currentPage != 1 && previousPage != 1) { %>
                    <a href="?page=1">1</a>
                <% } %>

                <% if (currentPage === lastPage && lastPage > 3) { %>
                    <a>...</a>
                <% } %>

                <% if (hasPreviousPage) { %>
                    <a href="?page=<%= previousPage %>"><%= previousPage %></a>
                <% } %>

                <a href="?page=<%= currentPage %>" class="active"><%= currentPage %></a>

                <% if (hasNextPage) { %>
                <a href="?page=<%= nextPage %>"><%= nextPage %></a>
                <% } %>

                <% if (currentPage === 1 && lastPage > 3) { %>
                    <a>...</a>
                <% } %>

                <% if (lastPage !== currentPage && nextPage !== lastPage) { %>
                <a href="?page=<%= lastPage %>"><%= lastPage %></a>
                <% } %>
            </section>
            <% } else { %>
                <h1>No therapists Available.</h1>
            <% } %> -->
    </main>





</body>
</html>